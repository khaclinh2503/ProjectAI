{
  "name": "bloom-tap-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "jsdom": "^29.0.0",
    "vitest": "^3.2.4"
  }
}
