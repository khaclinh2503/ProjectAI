---
phase: 14-lobby-leaderboard-ui
plan: "02"
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - BloomTap/assets/scene/LobbyScene.scene
  - BloomTap/assets/scene/LeaderboardScene.scene
autonomous: false
requirements:
  - LOBBY-01
  - "PLAYER-01 (UI side)"
  - "LB-01 (display side)"

must_haves:
  truths:
    - "LobbyScene renders with title, greeting, and 5 buttons in Cocos Preview"
    - "Name overlay appears on first run when no stored name exists"
    - "Entering a name and tapping Bat Dau closes overlay and shows greeting"
    - "Tapping Vuon Hoa, Tui Do, or Setting shows Sap ra mat toast without scene change"
    - "Tapping BXH navigates to LeaderboardScene"
    - "LeaderboardScene shows top-10 entries or empty state message"
    - "Back button on LeaderboardScene returns to LobbyScene"
  artifacts:
    - path: "BloomTap/assets/scene/LobbyScene.scene"
      provides: "Lobby scene file with all nodes and @property wiring"
    - path: "BloomTap/assets/scene/LeaderboardScene.scene"
      provides: "Leaderboard scene file with rows and @property wiring"
  key_links:
    - from: "BloomTap/assets/scene/LobbyScene.scene"
      to: "BloomTap/assets/scripts/LobbyController.ts"
      via: "@property bindings in Cocos Inspector"
      pattern: "LobbyController component attached to Canvas"
    - from: "BloomTap/assets/scene/LeaderboardScene.scene"
      to: "BloomTap/assets/scripts/LeaderboardController.ts"
      via: "@property bindings in Cocos Inspector"
      pattern: "LeaderboardController component attached to Canvas"
---

<objective>
Create both Cocos scene files in the Editor, wire all @property bindings to the controllers from Plan 01, and verify the complete lobby and leaderboard UI works end-to-end.

Purpose: The TypeScript controllers exist but need scene files with the correct node hierarchy and Inspector bindings to function. This plan creates those scenes and verifies everything works.

Output: Two functioning Cocos scenes (LobbyScene, LeaderboardScene) playable in Cocos Preview.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-lobby-leaderboard-ui/14-CONTEXT.md
@.planning/phases/14-lobby-leaderboard-ui/14-UI-SPEC.md
@.planning/phases/14-lobby-leaderboard-ui/14-01-SUMMARY.md
@BloomTap/assets/scripts/LobbyController.ts
@BloomTap/assets/scripts/LeaderboardController.ts
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Create LobbyScene and LeaderboardScene in Cocos Editor</name>
  <files>BloomTap/assets/scene/LobbyScene.scene, BloomTap/assets/scene/LeaderboardScene.scene</files>
  <action>
Human creates both scene files in Cocos Creator Editor. Claude cannot create .scene files (serialized JSON from Editor). Detailed instructions below.

**Create LobbyScene.scene in Cocos Editor:**

1. File > New Scene. Save as `BloomTap/assets/scene/LobbyScene.scene`.
2. Build the node tree under Canvas (match UI-SPEC exactly):

```
Canvas
  Background (empty Node, add UITransform: full canvas size)
    TitleLabel          — Label component, text="BLOOM TAP", fontSize=40, bold
    GreetingLabel       — Label component, text="Xin chao,  ", fontSize=18
    ButtonGroup (Node, optional Layout component: Vertical, spacing=8)
      BtnChoiNgay       — Node + Button + Label("Choi Ngay", fontSize=20, semibold)
      BtnVuonHoa        — Node + Button + Label("Vuon Hoa", fontSize=20)
      BtnTuiDo          — Node + Button + Label("Tui Do", fontSize=20)
      BtnBXH            — Node + Button + Label("BXH", fontSize=20)
      BtnSetting        — Node + Button + Label("Setting", fontSize=20)
    ToastLabel          — Label component, text="Sap ra mat!", fontSize=18, add UIOpacity component (opacity=255). Set node active=false.
    NameInputOverlay    — Node, add UIOpacity component (opacity=255). Set node active=true.
      DimBackground     — Node + Sprite or UITransform, Color(0,0,0,160), full canvas
      OverlayPanel      — Node, centered, secondary color Color(55,40,75,255)
        OverlayTitle    — Label, text="Nhap ten cua ban", fontSize=24
        NameEditBox     — EditBox component, maxLength=12, fontSize=18
        BtnBatDau       — Node + Button + Label("Bat Dau", fontSize=20)
```

3. Add LobbyController component to Canvas node.
4. Wire ALL @property bindings in Inspector:
   - titleLabel -> TitleLabel node's Label
   - greetingLabel -> GreetingLabel node's Label
   - btnChoiNgay -> BtnChoiNgay node's Button
   - btnVuonHoa -> BtnVuonHoa node's Button
   - btnTuiDo -> BtnTuiDo node's Button
   - btnBXH -> BtnBXH node's Button
   - btnSetting -> BtnSetting node's Button
   - nameInputOverlay -> NameInputOverlay node
   - nameEditBox -> NameEditBox node's EditBox
   - btnBatDau -> BtnBatDau node's Button
   - toastLabel -> ToastLabel node's Label

5. Set colors per UI-SPEC:
   - Background: Color(30,20,45,255) — dark purple
   - BtnChoiNgay: accent Color(255,220,60,255) fill
   - Other 4 buttons: Color(70,55,95,255) secondary fill
   - Button labels: white Color(255,255,255,255)
   - DimBackground: Color(0,0,0,160)
   - OverlayPanel: Color(55,40,75,255)

6. Button sizing: height 48px (2xl), width ~200px or stretch to fit. Touch target >= 44px.

**Create LeaderboardScene.scene in Cocos Editor:**

1. File > New Scene. Save as `BloomTap/assets/scene/LeaderboardScene.scene`.
2. Build the node tree:

```
Canvas
  Background (UITransform: full canvas, Color(30,20,45,255))
    TitleLabel          — Label, text="BANG XEP HANG", fontSize=40, bold
    BtnBack             — top-left, Node + Button + Label("Quay lai", fontSize=20)
    EntryList (Node, container)
      Row0              — Node, height 40px
        RankLabel       — Label, fontSize=18 (child name MUST be "RankLabel")
        NameLabel       — Label, fontSize=18 (child name MUST be "NameLabel")
        ScoreLabel      — Label, fontSize=18 (child name MUST be "ScoreLabel")
      Row1 through Row9 — duplicate Row0 structure (10 total rows)
    EmptyStateLabel     — Label, text="Chua co ai len bang. Hay choi ngay!", fontSize=18, centered. Set active=false.
```

3. Add LeaderboardController component to Canvas node.
4. Wire @property bindings in Inspector:
   - btnBack -> BtnBack node's Button
   - rows -> drag Row0..Row9 into the array (IN ORDER: Row0 first, Row9 last)
   - emptyStateLabel -> EmptyStateLabel node's Label

5. CRITICAL: Verify rows array order matches Row0-Row9 by checking Inspector array indices.

**Both scenes:** Add to Build Settings (Project > Build > Scene List) so `director.loadScene()` can find them.
  </action>
  <verify>ls BloomTap/assets/scene/LobbyScene.scene BloomTap/assets/scene/LeaderboardScene.scene</verify>
  <done>Both LobbyScene.scene and LeaderboardScene.scene exist in BloomTap/assets/scene/ with all @property bindings wired to LobbyController and LeaderboardController respectively. Both scenes added to Build Settings.</done>
  <read_first>
    - BloomTap/assets/scripts/LobbyController.ts (verify @property names to wire)
    - BloomTap/assets/scripts/LeaderboardController.ts (verify @property names to wire)
    - .planning/phases/14-lobby-leaderboard-ui/14-UI-SPEC.md (node tree reference)
  </read_first>
  <acceptance_criteria>
    - File exists: BloomTap/assets/scene/LobbyScene.scene
    - File exists: BloomTap/assets/scene/LeaderboardScene.scene
    - LobbyController component attached to LobbyScene Canvas
    - LeaderboardController component attached to LeaderboardScene Canvas
    - All 11 LobbyController @property bindings wired in Inspector
    - All 4 LeaderboardController @property bindings wired (btnBack, rows[0..9], emptyStateLabel)
  </acceptance_criteria>
  <resume-signal>Type "scenes created" when both LobbyScene and LeaderboardScene are saved with all @property bindings wired in the Cocos Editor.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify lobby and leaderboard UI in Cocos Preview</name>
  <files>BloomTap/assets/scene/LobbyScene.scene, BloomTap/assets/scene/LeaderboardScene.scene</files>
  <action>
Human verifies all Phase 14 success criteria in Cocos Preview. No code changes — pure verification.

Test checklist:
1. Name overlay on first run (clear localStorage, reopen LobbyScene)
2. Bat Dau button disabled when EditBox empty, enabled when >=1 char
3. Confirm name: overlay fades, greeting updates
4. 5 lobby buttons present and functional
5. Toast for Vuon Hoa, Tui Do, Setting
6. BXH navigates to LeaderboardScene
7. LeaderboardScene shows entries or empty state
8. Back button returns to LobbyScene
9. Name persists across reload
  </action>
  <verify>Human play-test in Cocos Preview confirms all 9 checklist items pass.</verify>
  <done>All Phase 14 success criteria (SC-1 through SC-5 from ROADMAP) verified in Cocos Preview. Name overlay, button navigation, toast, leaderboard display, and name persistence all work correctly.</done>
  <read_first>
    - .planning/phases/14-lobby-leaderboard-ui/14-CONTEXT.md (decisions D-01 through D-15 to verify)
    - .planning/ROADMAP.md (Phase 14 SC-1 through SC-5)
  </read_first>
  <acceptance_criteria>
    - SC-1: LobbyScene has 5 visible buttons with correct labels (Choi Ngay, Vuon Hoa, Tui Do, BXH, Setting)
    - SC-2: Tapping Vuon Hoa/Tui Do/Setting shows toast, no scene change, no crash
    - SC-3: First run with no stored name shows overlay; name persists after entry
    - SC-4: Greeting label displays stored player name
    - SC-5: BXH opens LeaderboardScene with rank/name/score rows or empty state message
  </acceptance_criteria>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
1. LobbyScene.scene and LeaderboardScene.scene exist in BloomTap/assets/scene/
2. Both scenes are in Build Settings scene list
3. LobbyController attached to LobbyScene Canvas with all 11 @property bindings wired
4. LeaderboardController attached to LeaderboardScene Canvas with all 4 @property bindings wired
5. Human verified: name overlay, 5 buttons, toast, leaderboard display, back navigation, name persistence
</verification>

<success_criteria>
- LobbyScene renders correctly in Cocos Preview with all UI elements
- Name input overlay works end-to-end (first run detection, validation, save, fade out, greeting update)
- All 5 lobby buttons function correctly (2 navigate, 3 show toast)
- LeaderboardScene shows entries or empty state correctly
- Back button returns to LobbyScene
- Player name persists across page reloads
- All Phase 14 success criteria (SC-1 through SC-5 from ROADMAP) verified
</success_criteria>

<output>
After completion, create `.planning/phases/14-lobby-leaderboard-ui/14-02-SUMMARY.md`
</output>
