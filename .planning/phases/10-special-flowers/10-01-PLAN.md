---
phase: 10-special-flowers
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - BloomTap/assets/scripts/logic/PowerUpState.ts
  - BloomTap/assets/scripts/logic/PowerUpState.test.ts
  - BloomTap/assets/scripts/logic/Grid.ts
  - BloomTap/assets/scripts/logic/Grid.test.ts
  - BloomTap/assets/scripts/logic/GameState.ts
  - BloomTap/assets/scripts/logic/GameState.test.ts
  - BloomTap/assets/scripts/logic/GameConfig.ts
  - BloomTap/assets/scripts/logic/GameConfig.test.ts
  - BloomTap/assets/resources/config/flowers.json
autonomous: true
requirements: [SPECIAL-01, SPECIAL-02, SPECIAL-03, SPECIAL-04]

must_haves:
  truths:
    - "PowerUpState tracks 3 independent effect expiry timestamps and answers isActive/getRemaining/getScoreMultiplier queries correctly"
    - "PowerUpState.shiftExpiries shifts all 5 fields (3 expiries + lastSpecialSpawnMs + stored multiplier is unaffected) for pause compatibility"
    - "PowerUpState.needsPitySpawn returns true when elapsed since last special spawn >= pityWindowMs"
    - "Cell interface has isSpecial and specialEffect fields; clearCell and clearAll reset both"
    - "GameState.applyCorrectTap accepts optional powerUpMultiplier parameter and multiplies score correctly"
    - "GameConfig.parseGameConfig validates powerUps section from flowers.json with all required fields"
    - "applySlowGrowthConfig returns a new FlowerTypeConfig with all duration fields scaled by factor without mutating original"
  artifacts:
    - path: "BloomTap/assets/scripts/logic/PowerUpState.ts"
      provides: "Pure logic class for power-up effect tracking"
      exports: ["PowerUpState", "SpecialEffectType", "applySlowGrowthConfig"]
    - path: "BloomTap/assets/scripts/logic/PowerUpState.test.ts"
      provides: "Comprehensive Vitest tests for PowerUpState"
      min_lines: 100
    - path: "BloomTap/assets/scripts/logic/Grid.ts"
      provides: "Cell interface with isSpecial + specialEffect fields"
      contains: "isSpecial"
    - path: "BloomTap/assets/scripts/logic/GameState.ts"
      provides: "applyCorrectTap with powerUpMultiplier parameter"
      contains: "powerUpMultiplier"
    - path: "BloomTap/assets/scripts/logic/GameConfig.ts"
      provides: "PowerUpsConfig interface + parseGameConfig with powerUps validation"
      exports: ["PowerUpsConfig", "parseGameConfig"]
    - path: "BloomTap/assets/resources/config/flowers.json"
      provides: "powerUps config block with specialChance, pityWindowMs, effect durations"
      contains: "powerUps"
  key_links:
    - from: "BloomTap/assets/scripts/logic/PowerUpState.ts"
      to: "BloomTap/assets/scripts/logic/Grid.ts"
      via: "SpecialEffectType used in Cell.specialEffect"
      pattern: "specialEffect.*SpecialEffectType"
    - from: "BloomTap/assets/scripts/logic/GameConfig.ts"
      to: "BloomTap/assets/resources/config/flowers.json"
      via: "parsePowerUps validates powerUps block"
      pattern: "powerUps"
---

<objective>
Create all pure-logic foundations for the special flowers system using TDD: PowerUpState class, Cell interface extension, GameState multiplier param, GameConfig powerUps schema, and applySlowGrowthConfig helper. All code is in the logic/ tier (no cc imports), fully testable with Vitest.

Purpose: Establish tested contracts that Wave 2 (GameController wiring + GridRenderer) will consume. Every behavior is proven by unit tests before any Cocos integration.
Output: 5 modified/new files in logic/ tier + updated flowers.json, all tests green.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-special-flowers/10-CONTEXT.md
@.planning/phases/10-special-flowers/10-RESEARCH.md

@BloomTap/assets/scripts/logic/Grid.ts
@BloomTap/assets/scripts/logic/GameState.ts
@BloomTap/assets/scripts/logic/GameConfig.ts
@BloomTap/assets/scripts/logic/FlowerTypes.ts
@BloomTap/assets/resources/config/flowers.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PowerUpState class + SpecialEffectType + applySlowGrowthConfig + Cell extension + GameState multiplier + config schema — RED then GREEN</name>
  <files>
    BloomTap/assets/scripts/logic/PowerUpState.ts,
    BloomTap/assets/scripts/logic/PowerUpState.test.ts,
    BloomTap/assets/scripts/logic/Grid.ts,
    BloomTap/assets/scripts/logic/Grid.test.ts,
    BloomTap/assets/scripts/logic/GameState.ts,
    BloomTap/assets/scripts/logic/GameState.test.ts,
    BloomTap/assets/scripts/logic/GameConfig.ts,
    BloomTap/assets/scripts/logic/GameConfig.test.ts,
    BloomTap/assets/resources/config/flowers.json
  </files>
  <read_first>
    BloomTap/assets/scripts/logic/Grid.ts,
    BloomTap/assets/scripts/logic/Grid.test.ts,
    BloomTap/assets/scripts/logic/GameState.ts,
    BloomTap/assets/scripts/logic/GameState.test.ts,
    BloomTap/assets/scripts/logic/GameConfig.ts,
    BloomTap/assets/scripts/logic/GameConfig.test.ts,
    BloomTap/assets/scripts/logic/FlowerTypes.ts,
    BloomTap/assets/resources/config/flowers.json
  </read_first>
  <behavior>
    PowerUpState tests (PowerUpState.test.ts):
    - activate(SCORE_MULTIPLIER, 1000, 6000, 3) sets expiry at 7000; isActive(SCORE_MULTIPLIER, 6999) === true; isActive(SCORE_MULTIPLIER, 7000) === false
    - activate(TIME_FREEZE, 1000, 5000) sets expiry at 6000; isActive(TIME_FREEZE, 5999) === true; isActive(TIME_FREEZE, 6000) === false
    - activate(SLOW_GROWTH, 1000, 8000) sets expiry at 9000; isActive(SLOW_GROWTH, 8999) === true; isActive(SLOW_GROWTH, 9000) === false
    - D-08: all 3 effects active simultaneously — activate all 3, getActiveCount returns 3
    - D-09: same-type reactivation resets timer — activate(SM, 1000, 6000, 3), activate(SM, 5000, 6000, 5), expiry is 11000 and multiplier is 5
    - D-10: different-type activation alongside existing — activate SM then TF, both active
    - getScoreMultiplier() returns stored multiplier value when SCORE_MULTIPLIER active, 1 when inactive
    - getRemaining(SCORE_MULTIPLIER, 3000) returns 4000 when expiry is 7000; returns 0 when expired
    - shiftExpiries(500) shifts all 3 expiry fields + lastSpecialSpawnMs by 500
    - needsPitySpawn(31000, 30000) returns true when lastSpecialSpawnMs is 0; returns false when lastSpecialSpawnMs is 5000
    - recordSpecialSpawn(5000) updates lastSpecialSpawnMs to 5000
    - reset(1000) zeros all expiries and sets lastSpecialSpawnMs to 1000 (not 0 — avoids pity firing immediately)
    - applySlowGrowthConfig: given base config with cycleDurationMs=3000, budMs=1350, etc. and factor=2.0, returns config with cycleDurationMs=6000, budMs=2700, etc. — all 7 duration fields doubled via Math.round
    - applySlowGrowthConfig does NOT mutate the original config object

    Grid tests (extend Grid.test.ts):
    - New cell has isSpecial === false and specialEffect === null
    - After setting cell.isSpecial = true and cell.specialEffect = 'SCORE_MULTIPLIER', clearCell resets both to false/null
    - clearAll resets isSpecial and specialEffect on all cells

    GameState tests (extend GameState.test.ts):
    - applyCorrectTap(100, combo) with no powerUpMultiplier works as before (backward compatible)
    - applyCorrectTap(100, combo, 3) applies Math.round(100 * 1.0 * 3) = 300 to score

    GameConfig tests (extend GameConfig.test.ts):
    - parseGameConfig with valid powerUps block returns PowerUpsConfig with specialChance=0.08, pityWindowMs=30000, scoreMultiplier.durationMs=6000, etc.
    - parseGameConfig with missing powerUps block throws "[GameConfig] flowersData must have a 'powerUps' key"
    - parseGameConfig with specialChance=-1 throws "[GameConfig] powerUps.specialChance must be >= 0"
    - parseGameConfig with missing multiplierByPhase throws descriptive error
    - parseGameConfig with multiplierByPhase length !== 3 throws "[GameConfig] powerUps.scoreMultiplier.multiplierByPhase must have exactly 3 elements"
  </behavior>
  <action>
    **RED phase — write all tests first, verify they fail:**

    1. Create `BloomTap/assets/scripts/logic/PowerUpState.ts` with ONLY type exports initially:
       ```typescript
       export type SpecialEffectType = 'SCORE_MULTIPLIER' | 'TIME_FREEZE' | 'SLOW_GROWTH';
       ```
       Leave PowerUpState class and applySlowGrowthConfig as stubs that throw "not implemented".

    2. Create `BloomTap/assets/scripts/logic/PowerUpState.test.ts` with all test cases from behavior block above. Import PowerUpState and applySlowGrowthConfig. Tests MUST fail (RED).

    3. Extend `BloomTap/assets/scripts/logic/Grid.test.ts` — add describe block "Cell isSpecial and specialEffect" with 3 tests from behavior block. Tests MUST fail because Cell interface doesn't have isSpecial yet.

    4. Extend `BloomTap/assets/scripts/logic/GameState.test.ts` — add describe block "applyCorrectTap with powerUpMultiplier" with 2 tests. Tests MUST fail because applyCorrectTap doesn't accept 3rd param yet.

    5. Extend `BloomTap/assets/scripts/logic/GameConfig.test.ts` — add describe block "powerUps config parsing" with 5 tests. Update existing valid-config test fixtures to include powerUps block. Tests MUST fail because parseGameConfig doesn't parse powerUps yet.

    6. Run `npx vitest run` — confirm new tests fail, existing tests still pass.

    **GREEN phase — implement to make tests pass:**

    7. `PowerUpState.ts` — implement full class per RESEARCH.md Pattern 2:
       - Private fields: `_scoreMultiplierExpiryMs = 0`, `_timeFreezeExpiryMs = 0`, `_slowGrowthExpiryMs = 0`, `_lastSpecialSpawnMs = 0`, `_scoreMultiplierValue = 1`
       - `activate(effect, nowMs, durationMs, multiplierValue?)`: switch on effect type, set expiry = nowMs + durationMs. For SCORE_MULTIPLIER also store multiplierValue (default 1). Per D-09: same type resets timer.
       - `isActive(effect, nowMs): boolean`: return expiry > nowMs for the matching field
       - `getActiveCount(nowMs): number`: count how many of the 3 expiries > nowMs
       - `getScoreMultiplier(): number`: return `_scoreMultiplierValue` (caller checks isActive first; but also safe to return 1 if not active)
       - `getRemaining(effect, nowMs): number`: return Math.max(0, expiry - nowMs)
       - `shiftExpiries(deltaMs)`: add deltaMs to all 4 fields (_scoreMultiplierExpiryMs, _timeFreezeExpiryMs, _slowGrowthExpiryMs, _lastSpecialSpawnMs)
       - `needsPitySpawn(nowMs, pityWindowMs): boolean`: return (nowMs - _lastSpecialSpawnMs) >= pityWindowMs
       - `recordSpecialSpawn(nowMs)`: set _lastSpecialSpawnMs = nowMs
       - `reset(sessionStartMs)`: zero all 3 expiries, set _lastSpecialSpawnMs = sessionStartMs, _scoreMultiplierValue = 1

       Export `applySlowGrowthConfig(base: FlowerTypeConfig, factor: number): FlowerTypeConfig`:
       ```typescript
       return {
           ...base,
           cycleDurationMs: Math.round(base.cycleDurationMs * factor),
           budMs: Math.round(base.budMs * factor),
           tapWindowMs: Math.round(base.tapWindowMs * factor),
           bloomingMs: Math.round(base.bloomingMs * factor),
           fullBloomMs: Math.round(base.fullBloomMs * factor),
           wiltingMs: Math.round(base.wiltingMs * factor),
           deadMs: Math.round(base.deadMs * factor),
       };
       ```
       Import FlowerTypeConfig from './FlowerTypes'. NO cc imports.

    8. `Grid.ts` — extend Cell interface:
       ```typescript
       export interface Cell {
           index: number;
           row: number;
           col: number;
           flower: FlowerFSM | null;
           isSpecial: boolean;
           specialEffect: SpecialEffectType | null;
       }
       ```
       Import SpecialEffectType from './PowerUpState'.
       Update constructor to initialize: `{ index, row, col, flower: null, isSpecial: false, specialEffect: null }`.
       Update `clearCell(cell)` to also set `cell.isSpecial = false; cell.specialEffect = null;`.
       Update `clearAll()` to also reset `cell.isSpecial = false; cell.specialEffect = null;` for each cell.

    9. `GameState.ts` — update `applyCorrectTap` signature:
       ```typescript
       applyCorrectTap(rawScore: number, combo: ComboSystem, powerUpMultiplier: number = 1): void {
           this.correctTaps += 1;
           this.score += Math.round(rawScore * combo.multiplier * powerUpMultiplier);
           combo.onCorrectTap();
           if (combo.tapCount > this.peakStreak) { this.peakStreak = combo.tapCount; }
       }
       ```
       Default value of 1 ensures full backward compatibility.

    10. `GameConfig.ts` — add PowerUpsConfig interface and parsing:
        ```typescript
        export interface PowerUpsConfig {
            specialChance: number;
            pityWindowMs: number;
            scoreMultiplier: {
                durationMs: number;
                multiplierByPhase: [number, number, number];
            };
            timeFreeze: {
                durationMs: number;
            };
            slowGrowth: {
                durationMs: number;
                factor: number;
            };
        }
        ```
        Add `powerUps: PowerUpsConfig` to `GameConfig` interface.
        Add `parsePowerUps(flowersData: unknown): PowerUpsConfig` function:
        - Validate `flowersData` has 'powerUps' key
        - `specialChance`: requireNonNegativeNumber (can be 0 to disable)
        - `pityWindowMs`: requirePositiveNumber
        - `scoreMultiplier.durationMs`: requirePositiveNumber
        - `scoreMultiplier.multiplierByPhase`: must be array of exactly 3 positive numbers
        - `timeFreeze.durationMs`: requirePositiveNumber
        - `slowGrowth.durationMs`: requirePositiveNumber
        - `slowGrowth.factor`: requirePositiveNumber
        Update `parseGameConfig` to call `parsePowerUps` and include in return object.

    11. `flowers.json` — add powerUps block after spawnPhases array:
        ```json
        "powerUps": {
            "specialChance": 0.08,
            "pityWindowMs": 30000,
            "scoreMultiplier": {
                "durationMs": 6000,
                "multiplierByPhase": [2, 3, 5]
            },
            "timeFreeze": {
                "durationMs": 5000
            },
            "slowGrowth": {
                "durationMs": 8000,
                "factor": 2.0
            }
        }
        ```

    12. Run `npx vitest run` — ALL tests (existing 186 + new) must pass.

    Commit RED: `test(10-01): add failing tests for PowerUpState, Cell.isSpecial, applyCorrectTap multiplier, powerUps config`
    Commit GREEN: `feat(10-01): implement PowerUpState, Cell extension, GameState multiplier, GameConfig powerUps schema`
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - PowerUpState.ts contains `export type SpecialEffectType = 'SCORE_MULTIPLIER' | 'TIME_FREEZE' | 'SLOW_GROWTH'`
    - PowerUpState.ts contains `export class PowerUpState`
    - PowerUpState.ts contains `export function applySlowGrowthConfig`
    - PowerUpState.ts does NOT contain `from 'cc'`
    - PowerUpState.test.ts contains at least 12 test cases (it/test calls)
    - Grid.ts Cell interface contains `isSpecial: boolean` and `specialEffect: SpecialEffectType | null`
    - Grid.ts clearCell contains `cell.isSpecial = false` and `cell.specialEffect = null`
    - Grid.ts clearAll contains `cell.isSpecial = false` and `cell.specialEffect = null`
    - Grid.ts constructor initializes cells with `isSpecial: false, specialEffect: null`
    - GameState.ts applyCorrectTap signature contains `powerUpMultiplier: number = 1`
    - GameState.ts applyCorrectTap body contains `* powerUpMultiplier`
    - GameConfig.ts contains `export interface PowerUpsConfig`
    - GameConfig.ts contains `powerUps: PowerUpsConfig` in GameConfig interface
    - flowers.json contains `"powerUps"` key with `"specialChance": 0.08`
    - `npx vitest run` exits with code 0 (all tests pass)
  </acceptance_criteria>
  <done>All PowerUpState behaviors tested and passing. Cell.isSpecial/specialEffect wired with reset discipline. GameState.applyCorrectTap accepts powerUpMultiplier. GameConfig validates powerUps section from JSON. flowers.json has powerUps config block. All 186+ tests green.</done>
</task>

</tasks>

<verification>
npx vitest run --reporter=verbose
# All tests pass including new PowerUpState, Grid isSpecial, GameState multiplier, GameConfig powerUps tests
</verification>

<success_criteria>
- PowerUpState class fully tested with all activate/isActive/getRemaining/shiftExpiries/needsPitySpawn/reset behaviors
- Cell interface extended with isSpecial and specialEffect, reset in clearCell and clearAll
- GameState.applyCorrectTap backward compatible with optional powerUpMultiplier
- GameConfig parses and validates powerUps config section from flowers.json
- applySlowGrowthConfig returns scaled config copy without mutating original
- All existing tests still pass (backward compatible changes)
</success_criteria>

<output>
After completion, create `.planning/phases/10-special-flowers/10-01-SUMMARY.md`
</output>
