---
phase: 03-renderer-and-input
plan: "03"
type: execute
wave: 3
depends_on:
  - "03-02"
files_modified: []
autonomous: false
requirements:
  - GRID-01
  - GRID-02
  - FLOW-03
  - GAME-01
  - GAME-02
  - GAME-03

must_haves:
  truths:
    - "All 5 Phase 3 success criteria from ROADMAP.md are visually confirmed by a human on device"
    - "No console errors during a 60-second manual play session"
    - "Grid renders correctly on both 375px and 430px viewport widths"
  artifacts:
    - path: ".planning/phases/03-renderer-and-input/03-03-SUMMARY.md"
      provides: "Phase 3 completion record with pass/fail per success criterion"
      contains: "Phase 3 complete"
  key_links: []
---

<objective>
Human verification checkpoint for Phase 3. All rendering and input work is done; this checkpoint confirms the 5 ROADMAP success criteria are satisfied before Phase 4 planning begins.

Purpose: Catch visual defects, touch latency issues, or viewport scaling problems that automated tests cannot detect. This is the gate before Phase 4 (Session Loop).
Output: Human confirms all 5 criteria pass, or reports specific failures for gap closure.
</objective>

<execution_context>
@C:/Users/Khac Linh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Khac Linh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-renderer-and-input/03-CONTEXT.md
@.planning/phases/03-renderer-and-input/03-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Visual and functional verification of Phase 3 in Cocos Creator preview</name>
  <action>
Human verifies all 5 Phase 3 ROADMAP success criteria by running the game in Cocos Creator browser preview and executing the test protocol below. No code changes are made in this task.
  </action>
  <verify>
    <automated>MISSING — This plan has no automated verify. All verification is manual per the protocol in what-built/how-to-verify below.</automated>
  </verify>
  <done>Human types "approved" after all 6 test cases pass, or reports specific failures with test numbers for gap closure planning.</done>
  <what-built>
    Complete Phase 3 renderer and input system:
    - 64 cell nodes pre-created at init, never created/destroyed during gameplay
    - 5 flower types with distinct hues (Cherry=red, Lotus=pink, Chrysanthemum=amber, Rose=violet, Sunflower=yellow)
    - 5 growth states with distinct brightness per type (BUD dim, BLOOMING medium, FULL_BLOOM bright, WILTING desaturated, DEAD very dark)
    - Tap BLOOMING → yellow flash 300ms → cell clears → score increases
    - Tap FULL_BLOOM → white flash 300ms → cell clears → score increases more
    - Tap BUD/WILTING/DEAD → red flash 150ms on that cell only → score decreases by 10 → combo resets
    - SpawnManager wired: flowers spawn every ~3s at Phase 1 rate
    - Debug score label shows current score and combo multiplier
  </what-built>
  <how-to-verify>
    **Setup:**
    1. Open Cocos Creator, open GameScene
    2. Click Preview (browser preview)
    3. Open browser DevTools console (F12) — watch for errors throughout

    **Test 1 — GRID-01: Object pool (all 64 nodes pre-created):**
    - In GameController.onLoad(), temporarily add: `console.log('Cell nodes created:', this.gridRenderer?._cellViews.length)`
    - Expected: "Cell nodes created: 64" appears once in console
    - Play for 30 seconds — the count should NOT appear again (no new node creation)
    - PASS if: log appears exactly once at startup

    **Test 2 — GRID-02: Grid scaling on 375px viewport:**
    - In browser DevTools, set device emulation to iPhone SE (375×667)
    - Reload preview
    - Expected: Grid fills approximately 80% of screen width (cells are large and tappable), no overflow
    - Set device emulation to iPhone 14 Pro Max (430×932)
    - Expected: Grid still fills ~80% width and is centered
    - PASS if: Grid is correctly scaled and centered on both viewports

    **Test 3 — FLOW-03: 5 states visually distinct:**
    - Watch the grid for 20 seconds without tapping
    - You should see: multiple distinct colors across cells (not all the same)
    - BUD cells should appear significantly dimmer than BLOOMING cells
    - FULL_BLOOM cells should be the brightest version of their color
    - WILTING cells should appear desaturated/grayed
    - DEAD cells should be very dark (barely visible)
    - PASS if: You can identify which state a flower is in by color alone without reading text

    **Test 4 — GAME-01/02: Correct tap scoring:**
    - Watch for a cell to reach BLOOMING state (medium brightness)
    - Note the current score in the debug label
    - Tap the BLOOMING cell
    - Expected: Yellow flash appears for ~300ms, cell becomes empty, score increases
    - Watch for a FULL_BLOOM cell (maximum brightness)
    - Note score, tap it
    - Expected: White flash, cell clears, score increases by MORE than the previous BLOOMING tap
    - PASS if: Both flash types appear, cell clears after flash, score delta is larger for FULL_BLOOM

    **Test 5 — GAME-03: Wrong tap feedback:**
    - Wait for a BUD cell (dim color, just spawned)
    - Tap it
    - Expected: Red flash on THAT CELL ONLY for ~150ms (not full screen), score decreases by 10, combo resets to x1.00 in debug label
    - PASS if: Red flash is cell-scoped, score decreases, combo resets

    **Test 6 — No errors:**
    - Play for 60 seconds total across tests above
    - Check DevTools console
    - PASS if: No red error messages (yellow warnings acceptable)
  </how-to-verify>
  <resume-signal>
    Type one of:
    - "approved" — all 6 tests pass, Phase 3 complete
    - "fail: [test numbers and description]" — e.g., "fail: Test 3 — WILTING and DEAD look identical; Test 5 — flash is full-screen not cell-scoped"
  </resume-signal>
</task>

</tasks>

<verification>
Human confirmation received with "approved" or specific failure descriptions documented for gap closure.
</verification>

<success_criteria>
All 5 ROADMAP Phase 3 success criteria confirmed by human:
1. All 64 grid cells render as pre-created GameObjects; none created/destroyed during gameplay
2. Each flower cell displays visually distinct appearance for all 5 growth states — distinguishable without text
3. Tapping BLOOMING or FULL_BLOOM triggers correct-tap path, removes flower, shows flash
4. Tapping BUD or WILTING/DEAD triggers wrong-tap path, shows red cell flash, deducts score
5. Grid scales correctly to fit at 375px-wide (iPhone SE) and 430px-wide (iPhone 14 Pro Max) viewport
</success_criteria>

<output>
After human approval, create `.planning/phases/03-renderer-and-input/03-03-SUMMARY.md` with:
- Pass/fail result per success criterion
- Any issues noted (even if passing overall)
- "Phase 3 complete" declaration if all criteria pass
</output>
