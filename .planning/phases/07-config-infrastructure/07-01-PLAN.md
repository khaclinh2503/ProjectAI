---
phase: 07-config-infrastructure
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - BloomTap/assets/scripts/logic/GameConfig.ts
  - BloomTap/assets/scripts/logic/GameConfig.test.ts
  - BloomTap/assets/resources/config/flowers.json
  - BloomTap/assets/resources/config/settings.json
autonomous: true
requirements: [CFG-01, CFG-02, CFG-03]

must_haves:
  truths:
    - "parseGameConfig() returns a valid GameConfig object when given well-formed flowers and settings data"
    - "parseGameConfig() throws a descriptive Error when any required field is missing, wrong type, or out of range"
    - "flowers.json contains all 5 flower types with values matching current hardcoded FLOWER_CONFIGS"
    - "settings.json contains session.durationMs=120000 and scoring.wrongTapPenalty=10"
  artifacts:
    - path: "BloomTap/assets/scripts/logic/GameConfig.ts"
      provides: "parseGameConfig() pure function + GameConfig type + validation helpers"
      exports: ["parseGameConfig", "GameConfig"]
    - path: "BloomTap/assets/scripts/logic/GameConfig.test.ts"
      provides: "Vitest tests for valid parse, invalid inputs, range violations"
      min_lines: 80
    - path: "BloomTap/assets/resources/config/flowers.json"
      provides: "5 flower types + 3 spawn phases data"
      contains: "CHERRY"
    - path: "BloomTap/assets/resources/config/settings.json"
      provides: "Session duration + wrong tap penalty"
      contains: "durationMs"
  key_links:
    - from: "BloomTap/assets/scripts/logic/GameConfig.test.ts"
      to: "BloomTap/assets/scripts/logic/GameConfig.ts"
      via: "import { parseGameConfig }"
      pattern: "import.*parseGameConfig.*from.*GameConfig"
    - from: "BloomTap/assets/scripts/logic/GameConfig.ts"
      to: "BloomTap/assets/scripts/logic/FlowerTypes.ts"
      via: "import FlowerTypeId, FlowerTypeConfig types"
      pattern: "import.*FlowerTypeId.*from.*FlowerTypes"
---

<objective>
Create the pure TypeScript `parseGameConfig()` function with full field-by-field validation, plus the two JSON config files containing values migrated from the current hardcoded constants.

Purpose: This is the data layer for config infrastructure — a pure function with zero Cocos dependencies that can be fully tested in Vitest. The JSON files become the single source of truth for game balance tuning.

Output: `GameConfig.ts` (parse function + types), `GameConfig.test.ts` (comprehensive tests), `flowers.json`, `settings.json`
</objective>

<execution_context>
@C:/Users/Khac Linh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Khac Linh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-config-infrastructure/07-01-CONTEXT.md
@.planning/phases/07-config-infrastructure/07-RESEARCH.md

<interfaces>
<!-- Key types the executor needs from existing codebase -->

From BloomTap/assets/scripts/logic/FlowerTypes.ts:
```typescript
export enum FlowerTypeId {
    CHERRY = 'CHERRY',
    LOTUS = 'LOTUS',
    CHRYSANTHEMUM = 'CHRYSANTHEMUM',
    ROSE = 'ROSE',
    SUNFLOWER = 'SUNFLOWER',
}

export interface FlowerTypeConfig {
    id: FlowerTypeId;
    cycleDurationMs: number;
    budMs: number;
    tapWindowMs: number;
    bloomingMs: number;
    fullBloomMs: number;
    wiltingMs: number;
    deadMs: number;
    scoreBloom: number;
    scoreFull: number;
}
```

From BloomTap/assets/scripts/logic/SpawnManager.ts:
```typescript
export interface SpawnPhaseConfig {
    startMs: number;
    endMs: number;
    intervalMs: number;
    maxAlive: number;
    weights: Record<FlowerTypeId, number>;
}
```

From BloomTap/assets/scripts/logic/GameState.ts:
```typescript
export const WRONG_TAP_PENALTY: number = 10;
export const SESSION_DURATION_MS: number = 120_000;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create JSON config files + GameConfig.ts parse function + GameConfig.test.ts</name>
  <files>
    BloomTap/assets/resources/config/flowers.json,
    BloomTap/assets/resources/config/settings.json,
    BloomTap/assets/scripts/logic/GameConfig.ts,
    BloomTap/assets/scripts/logic/GameConfig.test.ts
  </files>
  <read_first>
    BloomTap/assets/scripts/logic/FlowerTypes.ts,
    BloomTap/assets/scripts/logic/SpawnManager.ts,
    BloomTap/assets/scripts/logic/GameState.ts
  </read_first>
  <behavior>
    RED phase — write tests first:
    - Test 1: parseGameConfig(validFlowersData, validSettingsData) does not throw
    - Test 2: parseGameConfig returns flowers.CHERRY.cycleDurationMs === 3000
    - Test 3: parseGameConfig returns flowers.CHERRY.scoreFull === 120
    - Test 4: parseGameConfig returns all 5 flower type keys (CHERRY, LOTUS, CHRYSANTHEMUM, ROSE, SUNFLOWER)
    - Test 5: parseGameConfig returns 3 spawnPhases with correct intervalMs values (3000, 2000, 1000)
    - Test 6: parseGameConfig returns settings.session.durationMs === 120000
    - Test 7: parseGameConfig returns settings.scoring.wrongTapPenalty === 10
    - Test 8: parseGameConfig throws when flowersData is not an object (e.g., pass null or a string) — function accepts unknown, so non-object input must be rejected
    - Test 9: parseGameConfig throws when flowers.CHERRY.cycleDurationMs is 0 — error message contains "cycleDurationMs"
    - Test 10: parseGameConfig throws when flowers.CHERRY.cycleDurationMs is NaN — error message contains "cycleDurationMs"
    - Test 11: parseGameConfig throws when a required flower field is missing (e.g., no scoreBloom)
    - Test 12: parseGameConfig throws when settings.scoring.wrongTapPenalty is negative
    - Test 13: parseGameConfig does NOT throw when wrongTapPenalty is 0 (zero is valid)
    - Test 14: parseGameConfig does NOT throw when a spawn phase weight is 0 (zero excludes type)
    - Test 15: parseGameConfig throws when settings.session.durationMs is missing
    - Test 16: parseGameConfig throws when spawnPhases is missing from flowers data
    - Test 17: parseGameConfig throws when flowers key is missing from flowers data
    - Test 18: Each parsed FlowerTypeConfig has an `id` field matching its key (e.g., flowers.CHERRY.id === 'CHERRY')
  </behavior>
  <action>
    **TDD: RED phase first — write all tests, verify they fail, then GREEN phase.**

    **Step 1: Create `flowers.json`** at `BloomTap/assets/resources/config/flowers.json`.
    Create the `resources/config/` directory structure first.
    Migrate exact values from the hardcoded `FLOWER_CONFIGS` in FlowerTypes.ts and `PHASE_CONFIGS` in SpawnManager.ts.

    CRITICAL: The JSON does NOT include the `id` field for each flower — `parseGameConfig()` injects it from the key name.
    CRITICAL: The `endMs` values use `40000/80000/120000` per user-confirmed decision. SpawnManager.ts code uses strict `<` comparison (`elapsedMs < config.endMs`), so `endMs: 40000` means the phase covers `[startMs, 40000)` — i.e., 0ms to 39999ms inclusive.

    Exact `flowers.json` content:
    ```json
    {
      "flowers": {
        "CHERRY": {
          "cycleDurationMs": 3000, "budMs": 1350, "tapWindowMs": 900,
          "bloomingMs": 600, "fullBloomMs": 300, "wiltingMs": 450, "deadMs": 300,
          "scoreBloom": 80, "scoreFull": 120
        },
        "LOTUS": {
          "cycleDurationMs": 4500, "budMs": 1710, "tapWindowMs": 1800,
          "bloomingMs": 1200, "fullBloomMs": 600, "wiltingMs": 630, "deadMs": 360,
          "scoreBloom": 60, "scoreFull": 90
        },
        "CHRYSANTHEMUM": {
          "cycleDurationMs": 6000, "budMs": 1800, "tapWindowMs": 3000,
          "bloomingMs": 2000, "fullBloomMs": 1000, "wiltingMs": 780, "deadMs": 420,
          "scoreBloom": 40, "scoreFull": 60
        },
        "ROSE": {
          "cycleDurationMs": 8000, "budMs": 1840, "tapWindowMs": 4800,
          "bloomingMs": 3200, "fullBloomMs": 1600, "wiltingMs": 880, "deadMs": 480,
          "scoreBloom": 25, "scoreFull": 40
        },
        "SUNFLOWER": {
          "cycleDurationMs": 10000, "budMs": 1700, "tapWindowMs": 7000,
          "bloomingMs": 4670, "fullBloomMs": 2330, "wiltingMs": 800, "deadMs": 500,
          "scoreBloom": 15, "scoreFull": 25
        }
      },
      "spawnPhases": [
        {
          "startMs": 0, "endMs": 40000, "intervalMs": 3000, "maxAlive": 8,
          "weights": { "SUNFLOWER": 35, "ROSE": 30, "CHRYSANTHEMUM": 20, "LOTUS": 10, "CHERRY": 5 }
        },
        {
          "startMs": 40000, "endMs": 80000, "intervalMs": 2000, "maxAlive": 16,
          "weights": { "SUNFLOWER": 15, "ROSE": 20, "CHRYSANTHEMUM": 30, "LOTUS": 20, "CHERRY": 15 }
        },
        {
          "startMs": 80000, "endMs": 120000, "intervalMs": 1000, "maxAlive": 28,
          "weights": { "SUNFLOWER": 5, "ROSE": 10, "CHRYSANTHEMUM": 20, "LOTUS": 30, "CHERRY": 35 }
        }
      ]
    }
    ```

    **Step 2: Create `settings.json`** at `BloomTap/assets/resources/config/settings.json`:
    ```json
    {
      "session": { "durationMs": 120000 },
      "scoring": { "wrongTapPenalty": 10 }
    }
    ```

    **Step 3: Create `GameConfig.ts`** at `BloomTap/assets/scripts/logic/GameConfig.ts`.

    The function signature accepts `unknown` objects (not strings) per user-confirmed decision — avoids the JSON.stringify roundtrip when called from BootController with `JsonAsset.json` which is already a parsed object. Tests pass plain objects directly.

    ```typescript
    // GameConfig.ts — NO 'cc' import, pure TypeScript only
    import { FlowerTypeId, FlowerTypeConfig } from './FlowerTypes';
    import { SpawnPhaseConfig } from './SpawnManager';

    export interface GameConfig {
        flowers: Record<FlowerTypeId, FlowerTypeConfig>;
        spawnPhases: SpawnPhaseConfig[];
        settings: {
            session: { durationMs: number };
            scoring: { wrongTapPenalty: number };
        };
    }

    export function parseGameConfig(flowersData: unknown, settingsData: unknown): GameConfig { ... }
    ```

    Validation helpers inside GameConfig.ts:
    - `requirePositiveNumber(obj, key, context)` — throws if not number, NaN, or <= 0
    - `requireNonNegativeNumber(obj, key, context)` — throws if not number, NaN, or < 0

    Validation logic for flowers:
    - Check `flowersData` is an object with `flowers` and `spawnPhases` keys
    - For each of the 5 `FlowerTypeId` values, check the flower exists in `flowersData.flowers`
    - For each flower: validate all 9 numeric fields are positive numbers using `requirePositiveNumber`
    - Inject `id` field from the key name (cast string key to FlowerTypeId): `{ id: key as FlowerTypeId, ...validatedFields }`

    Validation logic for spawnPhases:
    - Check `spawnPhases` is an array
    - For each phase: validate `startMs` (>= 0), `endMs` (> 0), `intervalMs` (> 0), `maxAlive` (> 0)
    - For each phase: validate `weights` is an object, each value is `requireNonNegativeNumber`
    - Cast weight keys to FlowerTypeId in the output Record

    Validation logic for settings:
    - Check `settingsData` is an object with `session` and `scoring` keys
    - `session.durationMs` → `requirePositiveNumber`
    - `scoring.wrongTapPenalty` → `requireNonNegativeNumber`

    **Step 4: Create `GameConfig.test.ts`** at `BloomTap/assets/scripts/logic/GameConfig.test.ts`.
    Import `parseGameConfig` from `./GameConfig`.
    Create `validFlowersData` and `validSettingsData` as plain objects (not JSON strings) matching the JSON files above.
    Write all 18 tests from the behavior block above.
    For invalid-input tests, use spread + override pattern:
    ```typescript
    const badFlowers = { ...validFlowersData, flowers: { ...validFlowersData.flowers, CHERRY: { ...validFlowersData.flowers.CHERRY, cycleDurationMs: 0 } } };
    ```

    **TDD cycle:**
    1. Write all tests in GameConfig.test.ts
    2. Create a stub GameConfig.ts that exports the type and a function that throws "not implemented"
    3. Run tests — all should FAIL (RED)
    4. Implement parseGameConfig() fully
    5. Run tests — all should PASS (GREEN)
    6. Refactor if needed (REFACTOR)
  </action>
  <verify>
    <automated>cd E:/workspace/ProjectAI && npx vitest run BloomTap/assets/scripts/logic/GameConfig.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - GameConfig.test.ts contains `import { parseGameConfig } from './GameConfig'`
    - GameConfig.test.ts contains at least 15 `it(` test cases
    - GameConfig.ts contains `export function parseGameConfig(`
    - GameConfig.ts contains `export interface GameConfig`
    - GameConfig.ts does NOT contain `from 'cc'` (pure TypeScript, no Cocos imports)
    - flowers.json contains `"CHERRY"` and `"cycleDurationMs": 3000`
    - flowers.json contains `"spawnPhases"` with 3 array entries
    - flowers.json spawnPhases use endMs values 40000, 80000, 120000 (user-confirmed)
    - settings.json contains `"durationMs": 120000`
    - settings.json contains `"wrongTapPenalty": 10`
    - `npx vitest run BloomTap/assets/scripts/logic/GameConfig.test.ts` exits 0 with all tests passing
    - `npx vitest run` (full suite) exits 0 — all 150+ existing tests still pass
  </acceptance_criteria>
  <done>
    parseGameConfig() parses valid data correctly, throws descriptive errors on all invalid inputs, and all 150+ existing tests still pass alongside the new GameConfig tests.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run BloomTap/assets/scripts/logic/GameConfig.test.ts` — all new tests green
- `npx vitest run` — full suite (150+ tests) green, no regressions
- GameConfig.ts has zero `cc` imports (pure TypeScript)
- flowers.json values match current hardcoded FLOWER_CONFIGS and PHASE_CONFIGS exactly
</verification>

<success_criteria>
- parseGameConfig() correctly validates and returns typed config from valid input
- parseGameConfig() throws descriptive Error on every category of invalid input (missing field, wrong type, NaN, out of range)
- JSON files contain exact values matching current hardcoded constants
- Zero test regressions — all 150+ existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-config-infrastructure/07-01-SUMMARY.md`
</output>
