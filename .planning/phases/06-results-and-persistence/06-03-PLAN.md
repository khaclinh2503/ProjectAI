---
phase: 06-results-and-persistence
plan: 03
type: execute
wave: 3
depends_on:
  - 06-02
files_modified: []
autonomous: false
requirements:
  - RSLT-01
  - RSLT-02
  - RSLT-03

must_haves:
  truths:
    - "Results overlay shows Score, Highscore, Best combo, Accuracy when timer reaches 0"
    - "NEW BEST! label animates in after 0.5s on first positive score"
    - "Highscore persists across browser refresh"
    - "Play Again returns to start screen with all stats reset"
    - "NEW BEST! label is hidden when starting a new session"
  artifacts:
    - path: "BloomTap/assets/scripts/logic/StorageService.ts"
      provides: "localStorage persistence layer"
      exports: ["StorageService"]
    - path: "BloomTap/assets/scripts/GameController.ts"
      provides: "Full results + restart flow"
      contains: "highscoreLabel"
    - path: "BloomTap/assets/scene/GameScene.scene"
      provides: "4 result Label nodes wired"
      contains: "newBestLabel"
  key_links:
    - from: "browser localStorage"
      to: "StorageService.get('highscore')"
      via: "bloomtap_highscore key"
      pattern: "bloomtap_highscore"
---

<objective>
Human verification of the complete Phase 6 results and persistence flow in the browser. All three requirements (RSLT-01, RSLT-02, RSLT-03) require manual inspection because Cocos Creator scene node visibility and localStorage persistence cannot be verified headlessly.

Purpose: Gate before marking Phase 6 complete. Confirms the game works end-to-end as designed.
Output: All 4 ROADMAP Phase 6 success criteria confirmed by human tester.
</objective>

<execution_context>
@C:/Users/Khac Linh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Khac Linh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-results-and-persistence/06-CONTEXT.md
@.planning/phases/06-results-and-persistence/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-verification automated gate</name>
  <files></files>
  <read_first>
    - .planning/phases/06-results-and-persistence/06-02-SUMMARY.md (confirms Plan 02 complete)
  </read_first>
  <action>
    Run the full test suite to confirm baseline is green before opening the browser:
    ```
    cd /e/workspace/ProjectAI && npx vitest run
    ```
    If tests fail, stop and fix before proceeding to human verification.

    Then confirm all 4 scene nodes exist in GameScene.scene:
    ```
    grep -c "highscoreLabel\|bestComboLabel\|accuracyLabel\|newBestLabel" /e/workspace/ProjectAI/BloomTap/assets/scene/GameScene.scene
    ```
    Expected output: 4 (or more, one match per node name).
  </action>
  <verify>
    <automated>cd /e/workspace/ProjectAI && npx vitest run && grep -c "highscoreLabel" /e/workspace/ProjectAI/BloomTap/assets/scene/GameScene.scene</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0
    - `grep -c "highscoreLabel" BloomTap/assets/scene/GameScene.scene` returns >= 1
    - `grep -c "newBestLabel" BloomTap/assets/scene/GameScene.scene` returns >= 1
  </acceptance_criteria>
  <done>Full suite green. Scene file contains all 4 new label node names.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — full results and persistence flow</name>
  <what-built>
    Phase 6 complete results screen:
    - 4 new Label nodes in gameOverOverlay: highscoreLabel, bestComboLabel, accuracyLabel, newBestLabel
    - StorageService persists highscore to localStorage under key "bloomtap_highscore"
    - _triggerGameOver() reads/writes highscore and triggers "NEW BEST!" celebration on new best
    - onRestartTapped() zeroes correctTaps/wrongTaps/peakStreak and hides newBestLabel
  </what-built>
  <how-to-verify>
    Open the game in Cocos Creator Preview (or browser build). Run through these 4 test scenarios in order:

    **Scenario A — First run, no highscore (RSLT-01 + RSLT-03 zero state):**
    1. Open DevTools → Application → Storage → Local Storage → clear any "bloomtap_highscore" key
    2. Start a session. Let the 120s timer expire (or temporarily reduce SESSION_DURATION_MS to 5s for testing)
    3. Game-over overlay appears. Verify:
       - "Score: N" label is visible (N = your score)
       - Highscore row is NOT visible (hidden because no prior highscore)
       - "Best combo: N" label is visible
       - "X / Y taps (Z%)" accuracy label is visible
       - If score > 0: "NEW BEST!" label appears ~0.5s after overlay opens with scale-pop animation (yellow)
       - If score <= 0: "NEW BEST!" label does NOT appear

    **Scenario B — Persistence across refresh (RSLT-03):**
    1. After Scenario A with score > 0, note the score value
    2. Refresh the browser tab (F5)
    3. Start a new session and let it expire
    4. Verify: "Highscore: N" label IS visible and shows the value from Scenario A
    5. DevTools → Local Storage → confirm "bloomtap_highscore" key exists with correct value

    **Scenario C — Beating the highscore (RSLT-01):**
    1. From any session where you score higher than the stored highscore
    2. Verify: "NEW BEST!" label animates in after 0.5s
    3. Verify: "Highscore: N" shows the NEW higher value (updated, not the old stored value)

    **Scenario D — Restart resets state (RSLT-02):**
    1. On the game-over overlay (after "NEW BEST!" was shown), tap "Play Again"
    2. Verify: "NEW BEST!" label is NOT visible when start screen appears
    3. Complete another session
    4. Verify: score/combo/accuracy reset to fresh values (score starts at 0, combo at 1)
    5. Verify: the start screen shows correctly (no remnant labels from previous session)

    **ROADMAP Phase 6 success criteria checklist:**
    - [ ] Criteria 1: Results screen shows session score + all-time highscore when timer hits 0
    - [ ] Criteria 2: If session score > stored highscore, highscore updates and results screen reflects this
    - [ ] Criteria 3: Highscore persists across browser refresh and new sessions
    - [ ] Criteria 4: Restart button starts a new 120s session with all state reset (score 0, combo 1, fresh grid)
  </how-to-verify>
  <resume-signal>Type "approved" if all 4 ROADMAP criteria pass, or describe which scenario failed and what was observed.</resume-signal>
</task>

</tasks>

<verification>
Phase 6 is complete when:
1. `npx vitest run` exits 0 (150+ tests green)
2. Human tester confirms all 4 ROADMAP Phase 6 success criteria
3. No console errors during any verification scenario
</verification>

<success_criteria>
- All 4 Phase 6 ROADMAP success criteria confirmed by human
- Full test suite green (150+ passing)
- Phase 6 complete — Bloom Tap v1 feature complete
</success_criteria>

<output>
After completion, create `.planning/phases/06-results-and-persistence/06-03-SUMMARY.md`
</output>
