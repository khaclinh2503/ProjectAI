---
phase: 04-session-loop-and-scoring
plan: 04
type: execute
wave: 4
depends_on:
  - 04-03
files_modified: []
autonomous: false
requirements:
  - SESS-02
  - SESS-03
  - SESS-04
must_haves:
  truths:
    - "A complete 120-second game session runs from Start button to Game Over overlay"
    - "Countdown 3-2-1 displays clearly before session begins"
    - "Score, Timer, and Combo labels are all visible and updating during play"
    - "Spawn rate is visibly slower in 0-40s vs 80-120s (phase escalation observable)"
    - "Combo resets to 0 immediately on a wrong tap"
    - "Restart returns to Start Screen with empty grid and score=0"
  artifacts:
    - path: "BloomTap/assets/scene/GameScene.scene"
      provides: "Verified working scene — all Phase 4 success criteria confirmed"
  key_links:
    - from: "Start button tap"
      to: "3-2-1 countdown → session begins at T=0"
      via: "_onStartTapped → _startCountdown → _beginSession"
      pattern: "session start flow"
    - from: "T=120s"
      to: "Game Over overlay"
      via: "update() isGameOver check → _triggerGameOver"
      pattern: "game-over trigger"
---

<objective>
Human verification of the complete Phase 4 session loop. Claude opens Cocos Creator Preview and guides the human through a structured playthrough covering all 5 Phase 4 success criteria.

Purpose: Cocos scene rendering and interactive tap flows cannot be verified by Vitest. A human must observe the session loop end-to-end to confirm it meets requirements.

Output: Confirmed working game session OR list of specific issues for gap closure.
</objective>

<execution_context>
@C:/Users/Khac Linh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Khac Linh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-session-loop-and-scoring/04-CONTEXT.md
@.planning/phases/04-session-loop-and-scoring/04-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 4 session loop:
    - Start Screen with Start button (grid empty behind overlay)
    - 3-2-1 countdown after tapping Start
    - 120-second timer countdown in HUD
    - Score and Combo updating realtime on taps
    - 3-phase spawn rate escalation (slower 0-40s, faster 80-120s)
    - Game Over overlay at T=0 with final score
    - "Chơi lại" restart button returning to Start Screen
  </what-built>
  <how-to-verify>
    Open Cocos Creator, open the BloomTap project, click Play (browser preview).

    **Test 1 — Start Screen:**
    - [ ] Start Screen is visible with a Start button
    - [ ] Grid cells are visible behind overlay (empty, dark cells)
    - [ ] Timer "120" is NOT visible during Start Screen (HUD hidden)

    **Test 2 — Countdown:**
    - [ ] Tap "Start" → "3" appears large and centered
    - [ ] After ~1s → "2" appears
    - [ ] After ~1s → "1" appears
    - [ ] After ~1s → countdown disappears, HUD appears, flowers start spawning

    **Test 3 — Live HUD (during play):**
    - [ ] Score label (left) starts at 0 and increases on correct taps
    - [ ] Timer label (center) counts down: 120, 119, 118...
    - [ ] Combo label (right) shows "Combo x0" then "Combo x1", "Combo x2"... on correct taps
    - [ ] Combo resets to "Combo x0" immediately after a wrong tap (tap empty cell or bud)

    **Test 4 — Phase escalation (visual observation):**
    - [ ] 0-40s: Flowers spawn slowly (sparse grid, easy to track)
    - [ ] 80-120s: Flowers spawn rapidly (dense grid, harder to keep up)

    **Test 5 — Game Over:**
    - [ ] At T=0: Game Over overlay appears with "Game Over" title and "Score: XXXX"
    - [ ] Grid input is frozen (tapping cells has no effect after game over)
    - [ ] "Chơi lại" button is visible

    **Test 6 — Restart:**
    - [ ] Tap "Chơi lại" → Start Screen reappears
    - [ ] Grid is empty (no flowers from previous game)
    - [ ] Tapping Start again triggers another countdown and fresh session with Score: 0
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 6 tests pass.
    Or describe specific failures — e.g., "Timer shows 120 on start screen" or "Combo does not reset on wrong tap".
  </resume-signal>
</task>

</tasks>

<verification>
Human playthrough confirms all Phase 4 ROADMAP success criteria:
1. 120-second session runs start to game-over
2. Combo multiplier increases on correct taps; score reflects multiplier
3. Wrong tap resets combo to 0 immediately in HUD
4. Spawn rate visibly escalates across 3 phases
5. Score, Timer, Combo visible and updating throughout session
</verification>

<success_criteria>
All 6 test checkboxes above confirmed by human. Phase 4 complete.
</success_criteria>

<output>
After completion, create `.planning/phases/04-session-loop-and-scoring/04-04-SUMMARY.md`
</output>
