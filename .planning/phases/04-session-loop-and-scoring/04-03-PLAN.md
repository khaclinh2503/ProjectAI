---
phase: 04-session-loop-and-scoring
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - BloomTap/assets/scene/GameScene.scene
autonomous: true
requirements:
  - HUD-01
  - HUD-02
  - HUD-03
must_haves:
  truths:
    - "HUD row is visible above the grid with Score (left), Timer (center), Combo (right) labels"
    - "Start Screen overlay is visible when game loads — grid empty, no spawning, Start button present"
    - "Countdown overlay shows 3 then 2 then 1 after tapping Start (one per second)"
    - "Game Over overlay appears when timer reaches 0 with final score displayed"
    - "Chơi lai button on Game Over overlay resets game and returns to Start Screen"
    - "All @property refs on GameController are wired to the correct scene nodes in the inspector"
    - "HUD node starts inactive (active=false) so timer does not show during Start Screen"
  artifacts:
    - path: "BloomTap/assets/scene/GameScene.scene"
      provides: "Scene with HUD node, StartScreenOverlay, CountdownOverlay, GameOverOverlay — all wired to GameController @property fields"
      contains: "HUD"
  key_links:
    - from: "GameController @property startButton"
      to: "StartScreenOverlay StartButton node"
      via: "Cocos inspector property binding — click fires _onStartTapped"
      pattern: "startButton"
    - from: "GameController @property hudNode"
      to: "HUD node in scene"
      via: "Cocos inspector property binding — active toggled by session phase"
      pattern: "hudNode"
    - from: "GameController @property restartButton"
      to: "GameOverOverlay RestartButton node"
      via: "Cocos inspector property binding — click fires onRestartTapped"
      pattern: "restartButton"
---

<objective>
Add all required scene nodes to `GameScene.scene` and wire them to `GameController` @property fields via the Cocos Creator scene JSON format. After this plan the game is fully playable end-to-end in Cocos Creator Preview.

Purpose: The session state machine (04-02) already exists with null-guarded @property refs. This plan creates the actual nodes and wires them so those refs resolve at runtime.

Output: `GameScene.scene` gains HUD node (3 labels), StartScreenOverlay, CountdownOverlay, and GameOverOverlay (with Start and Restart buttons). All GameController @property fields are assigned.
</objective>

<execution_context>
@C:/Users/Khac Linh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Khac Linh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-session-loop-and-scoring/04-CONTEXT.md
@.planning/phases/04-session-loop-and-scoring/04-RESEARCH.md
@.planning/phases/04-session-loop-and-scoring/04-02-SUMMARY.md

<interfaces>
<!-- GameController @property fields that need scene wiring (from 04-02) -->

Fields to wire in GameController (all declared with @property):
```typescript
@property(GridRenderer) gridRenderer          — already wired from Phase 3
@property(Label)        debugScoreLabel       — Phase 3 debug; can leave as null (no node needed)
@property(Node)         hudNode               — HUD container node (active=false initially)
@property(Label)        scoreLabel            — child of HUD: "0" text, left side
@property(Label)        timerLabel            — child of HUD: "120" text, center
@property(Label)        comboLabel            — child of HUD: "Combo x0" text, right side
@property(Node)         startOverlay          — StartScreenOverlay node (active=true initially)
@property(Button)       startButton           — Button inside startOverlay
@property(Node)         countdownOverlay      — CountdownOverlay node (active=false initially)
@property(Label)        countdownLabel        — Label inside countdownOverlay showing "3"/"2"/"1"
@property(Node)         gameOverOverlay       — GameOverOverlay node (active=false initially)
@property(Label)        finalScoreLabel       — Label inside gameOverOverlay for final score
@property(Button)       restartButton         — Button inside gameOverOverlay ("Chơi lại")
```

Scene structure (from RESEARCH.md — confirmed architecture):
```
Canvas (existing)
├── GridContainer (existing — GridRenderer component)
├── HUD (Node, active=false) — NEW
│   ├── ScoreLabel (Label)   — left
│   ├── TimerLabel (Label)   — center
│   └── ComboLabel (Label)   — right
├── StartScreenOverlay (Node, active=true) — NEW
│   ├── TitleLabel (Label "Bloom Tap")
│   └── StartButton (Node with Button + UITransform + Label "Start")
├── CountdownOverlay (Node, active=false) — NEW
│   └── CountdownLabel (Label "3")
└── GameOverOverlay (Node, active=false) — NEW
    ├── GameOverTitle (Label "Game Over")
    ├── FinalScoreLabel (Label "Score: 0")
    └── RestartButton (Node with Button + UITransform + Label "Chơi lại")
```

Design resolution: 720x1280 portrait (confirmed from Phase 1).
HUD sits above the GridContainer. GridContainer Y position from Phase 3 is approximately
centered — HUD should be placed at a Y position above the grid top edge.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add HUD and overlay nodes to GameScene.scene</name>
  <files>BloomTap/assets/scene/GameScene.scene</files>
  <action>
    The GameScene.scene file is a Cocos Creator JSON scene. Read the existing file first to
    understand the current node UUID structure and Canvas children.

    Add the following nodes as children of Canvas (alongside the existing GridContainer):

    **HUD Node:**
    - Node name: "HUD"
    - active: false (hidden until PLAYING — per RESEARCH.md Pitfall 5)
    - Position: x=0, y=550 (above grid; 720x1280 canvas, grid centered around y=0)
    - Size: contentSize width=700, height=60
    - UITransform: width=700, height=60
    - Layout: horizontal — 3 label children spaced evenly
    - Children:
      - "ScoreLabel": cc.Label, string="0", fontSize=36, color=white (#FFFFFF), anchor=(0,0.5), position x=-220
      - "TimerLabel": cc.Label, string="120", fontSize=44, color=white (#FFFFFF), anchor=(0.5,0.5), position x=0
      - "ComboLabel": cc.Label, string="Combo x0", fontSize=32, color=white (#FFFFFF), anchor=(1,0.5), position x=220

    **StartScreenOverlay Node:**
    - Node name: "StartScreenOverlay"
    - active: true (visible initially)
    - Position: x=0, y=0
    - Size: contentSize width=720, height=1280 (full canvas)
    - Background: semi-transparent dark — set UIOpacity or use a Graphics node child with
      color rgba(0,0,0,180). (Use Claude's discretion: a simple sprite or Graphics rect is fine;
      full-coverage black-alpha node covers entire grid nicely.)
    - Children:
      - "TitleLabel": cc.Label, string="Bloom Tap", fontSize=72, color=white, anchor=(0.5,0.5), position y=200
      - "StartButton" node:
        - Position: x=0, y=-50
        - UITransform: width=240, height=80
        - cc.Button component attached
        - Child "Label": cc.Label, string="Start", fontSize=48, color=white

    **CountdownOverlay Node:**
    - Node name: "CountdownOverlay"
    - active: false
    - Position: x=0, y=0
    - Size: width=720, height=1280
    - Background: same dark semi-transparent as StartScreen
    - Children:
      - "CountdownLabel": cc.Label, string="3", fontSize=200, color=white, anchor=(0.5,0.5), position y=0
        (Large, centered — "must be large and clear so player sees it immediately" per CONTEXT.md specifics)

    **GameOverOverlay Node:**
    - Node name: "GameOverOverlay"
    - active: false
    - Position: x=0, y=0
    - Size: width=600, height=400 (centered overlay card, not full screen)
    - Background: dark rounded rect (use Sprite/Graphics — Claude's discretion on exact styling)
    - Children:
      - "GameOverTitle": cc.Label, string="Game Over", fontSize=64, color=white, position y=120
      - "FinalScoreLabel": cc.Label, string="Score: 0", fontSize=48, color=white, position y=20
      - "RestartButton" node:
        - Position: x=0, y=-100
        - UITransform: width=240, height=80
        - cc.Button component
        - Child "Label": cc.Label, string="Chơi lại", fontSize=44, color=white

    **Wire @property fields on GameController component:**
    After adding nodes, assign the following @property bindings in the GameController component
    node's inspector bindings (in the scene JSON, update the GameController component's
    serialized property array):
    - hudNode → HUD node
    - scoreLabel → ScoreLabel (child of HUD)
    - timerLabel → TimerLabel (child of HUD)
    - comboLabel → ComboLabel (child of HUD)
    - startOverlay → StartScreenOverlay node
    - startButton → StartButton node (the one with cc.Button component)
    - countdownOverlay → CountdownOverlay node
    - countdownLabel → CountdownLabel (child of CountdownOverlay)
    - gameOverOverlay → GameOverOverlay node
    - finalScoreLabel → FinalScoreLabel (child of GameOverOverlay)
    - restartButton → RestartButton node (the one with cc.Button component)

    IMPORTANT: Cocos Creator .scene files are JSON. Read the existing GameScene.scene first to
    understand the UUID format, the existing Canvas children structure, and the GameController
    component serialization format. Follow the exact same pattern for new nodes. All new node UUIDs
    must be freshly generated (use crypto.randomUUID() pattern or the existing UUID format in the
    file). Preserve all existing nodes (Canvas, GridContainer, camera, etc.) unchanged.
  </action>
  <verify>
    <automated>cd E:/workspace/ProjectAI && npx vitest run</automated>
  </verify>
  <done>
    GameScene.scene is valid JSON (parseable). HUD, StartScreenOverlay, CountdownOverlay, and
    GameOverOverlay nodes exist as Canvas children. GameController @property fields are assigned
    to the correct node UUIDs. Vitest suite still passes (scene change does not affect logic tests).
  </done>
</task>

</tasks>

<verification>
Scene JSON validity: `node -e "JSON.parse(require('fs').readFileSync('BloomTap/assets/scene/GameScene.scene','utf8'))"` (no error)
Vitest: `cd E:/workspace/ProjectAI && npx vitest run`

Full functional verification is in plan 04-04 (human checkpoint).
</verification>

<success_criteria>
- GameScene.scene parses as valid JSON
- HUD node (active=false), StartScreenOverlay (active=true), CountdownOverlay (active=false), GameOverOverlay (active=false) all present in Canvas children
- GameController @property bindings wired for all 11 new fields
- `npx vitest run` passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-session-loop-and-scoring/04-03-SUMMARY.md`
</output>
