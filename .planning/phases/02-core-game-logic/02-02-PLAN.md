---
phase: 02-core-game-logic
plan: 02
type: tdd
wave: 2
depends_on:
  - 02-01
files_modified:
  - assets/scripts/logic/ComboSystem.ts
  - assets/scripts/logic/SpawnManager.ts
  - assets/scripts/logic/ComboSystem.test.ts
  - assets/scripts/logic/SpawnManager.test.ts
autonomous: true
requirements:
  - FLOW-04
  - FLOW-01

must_haves:
  truths:
    - "ComboSystem.onCorrectTap() increments multiplier correctly and halves the step at tapCount thresholds 10, 50"
    - "ComboSystem.onWrongTap() resets multiplier to 1x, step to 0.5, tapCount to 0"
    - "SpawnManager.getPhaseConfig(elapsedMs) returns the correct config for all 3 session phases and the 120s boundary"
    - "SpawnManager.pickFlowerType(elapsedMs) returns only valid FlowerTypeId values"
    - "npm run test:run passes all tests including Plan 01 tests"
  artifacts:
    - path: "assets/scripts/logic/ComboSystem.ts"
      provides: "Multiplier counter: onCorrectTap, onWrongTap, applyToScore, multiplier getter, tapCount getter"
      exports: ["ComboSystem"]
    - path: "assets/scripts/logic/SpawnManager.ts"
      provides: "Phase config lookup + weighted random type picker"
      exports: ["SpawnManager", "SpawnPhaseConfig"]
  key_links:
    - from: "assets/scripts/logic/SpawnManager.ts"
      to: "assets/scripts/logic/FlowerTypes.ts"
      via: "import FlowerTypeId for weights and return type"
      pattern: "import.*FlowerTypes"
    - from: "assets/scripts/logic/ComboSystem.ts"
      to: "(no external logic imports)"
      via: "self-contained — no imports from other logic files needed"
      pattern: ""
---

<objective>
Implement ComboSystem and SpawnManager as pure TypeScript with full unit test coverage. These two systems complete the pure logic tier for Phase 2.

Purpose: ComboSystem defines the scoring multiplier mechanic. SpawnManager provides the phase-table-driven spawn configuration and weighted flower type selection. Both are consumed by Phase 4's session loop and must be testable now without any engine dependency.

Output: ComboSystem.ts, SpawnManager.ts, their test files — all passing with npm run test:run.
</objective>

<execution_context>
@C:/Users/Khac Linh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Khac Linh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-game-logic/02-CONTEXT.md
@.planning/phases/02-core-game-logic/02-RESEARCH.md
@.planning/phases/02-core-game-logic/02-01-SUMMARY.md

<interfaces>
<!-- From assets/scripts/logic/FlowerTypes.ts (created in Plan 01) -->
```typescript
export enum FlowerTypeId {
    CHERRY = 'CHERRY',
    LOTUS = 'LOTUS',
    CHRYSANTHEMUM = 'CHRYSANTHEMUM',
    ROSE = 'ROSE',
    SUNFLOWER = 'SUNFLOWER',
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ComboSystem</name>
  <files>
    assets/scripts/logic/ComboSystem.ts,
    assets/scripts/logic/ComboSystem.test.ts
  </files>
  <behavior>
    ComboSystem.test.ts:
    - new ComboSystem(): multiplier === 1, tapCount === 0
    - After 1 correct tap: multiplier === 1.5 (1 + 0.5)
    - After 2 correct taps: multiplier === 2.0
    - After wrong tap following 2 correct: multiplier === 1, tapCount === 0
    - After wrong tap: applyToScore(100) === 100 (multiplier back to 1x)
    - Step halving at tap 10: tap 10 uses step 0.5 -> multiplier increases by 0.5; tap 11 uses step 0.25 -> multiplier increases by 0.25
    - After 10 taps: multiplier === 1 + (10 * 0.5) === 6.0
    - After 11 taps: multiplier === 6.0 + 0.25 === 6.25
    - Step halving at tap 50: after 50 taps, tap 51 uses step 0.125
    - applyToScore(rawScore) === rawScore * multiplier
    - After reset, step is back to 0.5 (tap 1 after reset: multiplier = 1.5)
  </behavior>
  <action>
    RED phase: Write ComboSystem.test.ts first. Run npm run test:run -- ComboSystem.test.ts (must fail: cannot find module).

    GREEN phase: Implement ComboSystem.ts.

    Rules from CONTEXT.md (all values locked):
    - Init: multiplier = 1, step = 0.5, tapCount = 0
    - onCorrectTap(): multiplier += _step; tapCount += 1; then check thresholds — if tapCount === 10: _step = 0.25; if tapCount === 50: _step = 0.125. (tapCount > 100: step stays 0.125, no further change needed)
    - onWrongTap(): multiplier = 1; _step = 0.5; tapCount = 0
    - applyToScore(rawScore): return rawScore * _multiplier
    - Expose: get multiplier(), get tapCount() as readonly getters

    CRITICAL: Track _step as a mutable instance variable updated only at threshold crossings in onCorrectTap(). Do NOT compute step from tapCount range checks — this breaks after reset (see RESEARCH.md Pitfall 4).

    No import from 'cc'.
  </action>
  <verify>
    <automated>cd E:/workspace/ProjectAI && npm run test:run -- ComboSystem.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>All ComboSystem tests pass. multiplier starts at 1. step halves to 0.25 at tap 10 and to 0.125 at tap 50. Wrong tap fully resets state. applyToScore returns rawScore * multiplier.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SpawnManager</name>
  <files>
    assets/scripts/logic/SpawnManager.ts,
    assets/scripts/logic/SpawnManager.test.ts
  </files>
  <behavior>
    SpawnManager.test.ts:
    - getPhaseConfig(0): intervalMs === 3000, maxAlive === 8
    - getPhaseConfig(20000): intervalMs === 3000 (still Phase 1)
    - getPhaseConfig(39999): intervalMs === 3000 (still Phase 1)
    - getPhaseConfig(40000): intervalMs === 2000, maxAlive === 16 (Phase 2)
    - getPhaseConfig(60000): intervalMs === 2000 (Phase 2)
    - getPhaseConfig(80000): intervalMs === 1000, maxAlive === 28 (Phase 3)
    - getPhaseConfig(119999): intervalMs === 1000 (Phase 3)
    - getPhaseConfig(120000): returns last config (Phase 3) — defensive fallback, does not throw
    - pickFlowerType(0) returns a value from FlowerTypeId enum (valid type)
    - pickFlowerType(80000) returns a valid FlowerTypeId
    - Phase 1 weights: SUNFLOWER=35, ROSE=30, CHRYSANTHEMUM=20, LOTUS=10, CHERRY=5 (sum=100)
    - Phase 2 weights: SUNFLOWER=15, ROSE=20, CHRYSANTHEMUM=30, LOTUS=20, CHERRY=15 (sum=100)
    - Phase 3 weights: SUNFLOWER=5, ROSE=10, CHRYSANTHEMUM=20, LOTUS=30, CHERRY=35 (sum=100)
  </behavior>
  <action>
    RED phase: Write SpawnManager.test.ts first. Run npm run test:run -- SpawnManager.test.ts (must fail).

    GREEN phase: Implement SpawnManager.ts.

    SpawnPhaseConfig interface:
    ```typescript
    export interface SpawnPhaseConfig {
        startMs: number;
        endMs: number;
        intervalMs: number;
        maxAlive: number;
        weights: Record<FlowerTypeId, number>;
    }
    ```

    Define PHASE_CONFIGS as a module-level constant (not a class field) with 3 entries matching CONTEXT.md locked values exactly.

    getPhaseConfig(elapsedMs): iterate PHASE_CONFIGS, return first where elapsedMs >= startMs && elapsedMs < endMs. If no match (>= 120000), return PHASE_CONFIGS[2] (defensive fallback — RESEARCH.md Pitfall 5).

    pickFlowerType(elapsedMs): get config, build weighted-sum loop over Object.entries(config.weights), accumulate total, draw rand = Math.random() * total, iterate decrementing rand by weight, return first FlowerTypeId where rand <= 0. Return last entry as failsafe.

    Import FlowerTypeId from './FlowerTypes'. No import from 'cc'.
  </action>
  <verify>
    <automated>cd E:/workspace/ProjectAI && npm run test:run -- SpawnManager.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>All SpawnManager tests pass. getPhaseConfig returns correct config for all 3 phase time ranges and the 120s boundary. pickFlowerType returns only valid FlowerTypeId values. All weight tables sum to 100.</done>
</task>

</tasks>

<verification>
Run full Phase 2 test suite after both tasks:

```bash
cd E:/workspace/ProjectAI && npm run test:run 2>&1
```

All 5 test files must be green: FlowerTypes.test.ts, FlowerFSM.test.ts, Grid.test.ts, ComboSystem.test.ts, SpawnManager.test.ts.

Verify no 'cc' imports anywhere in logic/:
```bash
grep -r "from 'cc'" E:/workspace/ProjectAI/assets/scripts/logic/ 2>/dev/null
```

Must return empty.
</verification>

<success_criteria>
- npm run test:run exits green for all 5 test files
- ComboSystem: multiplier starts at 1x, increments by 0.5/0.25/0.125 at correct thresholds, resets fully on wrong tap
- SpawnManager: getPhaseConfig returns correct intervalMs and maxAlive for 0s, 40s, 80s, 120s boundaries; does not throw at 120s
- SpawnManager: all 3 phase weight tables sum to 100; pickFlowerType returns a valid FlowerTypeId
- Zero 'cc' imports in assets/scripts/logic/
- Pure logic tier is complete: FlowerTypes + FlowerState + FlowerFSM + Grid + ComboSystem + SpawnManager
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-game-logic/02-02-SUMMARY.md` using the summary template.
</output>
