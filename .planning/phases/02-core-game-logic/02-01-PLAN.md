---
phase: 02-core-game-logic
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - vitest.config.ts
  - tsconfig.test.json
  - assets/scripts/logic/FlowerTypes.ts
  - assets/scripts/logic/FlowerState.ts
  - assets/scripts/logic/FlowerFSM.ts
  - assets/scripts/logic/Grid.ts
  - assets/scripts/logic/FlowerTypes.test.ts
  - assets/scripts/logic/FlowerFSM.test.ts
  - assets/scripts/logic/Grid.test.ts
autonomous: true
requirements:
  - FLOW-01
  - FLOW-02
  - GRID-01
  - GRID-02

must_haves:
  truths:
    - "FlowerFSM.getState(nowMs) returns correct state for any injected timestamp — verifiable without a browser"
    - "All 5 FlowerTypeConfigs exist with exact locked values (cycle, tap window, scores)"
    - "Grid has exactly 64 cells; getRandomEmptyCell() returns a Cell or null; spawnFlower/clearCell mutate state correctly"
    - "npm run test:run passes all tests in under 10 seconds"
    - "No file under assets/scripts/logic/ imports from 'cc'"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Test runner config pointing at logic/**/*.test.ts"
      contains: "environment: 'node'"
    - path: "tsconfig.test.json"
      provides: "Separate tsconfig with vitest/globals; does not pollute Cocos main tsconfig"
      contains: "vitest/globals"
    - path: "assets/scripts/logic/FlowerTypes.ts"
      provides: "FlowerTypeId enum + FlowerTypeConfig interface + FLOWER_CONFIGS lookup table (all 5 types)"
      exports: ["FlowerTypeId", "FlowerTypeConfig", "FLOWER_CONFIGS"]
    - path: "assets/scripts/logic/FlowerState.ts"
      provides: "FlowerState enum with 6 values"
      exports: ["FlowerState"]
    - path: "assets/scripts/logic/FlowerFSM.ts"
      provides: "Timestamp-based FSM: getState(nowMs), getScore(nowMs), collect()"
      exports: ["FlowerFSM"]
    - path: "assets/scripts/logic/Grid.ts"
      provides: "64-cell flat array: getCell, getCells, getRandomEmptyCell, spawnFlower, clearCell, getAliveCount"
      exports: ["Grid", "Cell"]
  key_links:
    - from: "assets/scripts/logic/FlowerFSM.ts"
      to: "assets/scripts/logic/FlowerTypes.ts"
      via: "import FlowerTypeConfig"
      pattern: "import.*FlowerTypes"
    - from: "assets/scripts/logic/FlowerFSM.ts"
      to: "assets/scripts/logic/FlowerState.ts"
      via: "import FlowerState"
      pattern: "import.*FlowerState"
    - from: "assets/scripts/logic/Grid.ts"
      to: "assets/scripts/logic/FlowerFSM.ts"
      via: "Cell.flower: FlowerFSM | null"
      pattern: "import.*FlowerFSM"
    - from: "vitest.config.ts"
      to: "assets/scripts/logic/**/*.test.ts"
      via: "include glob pattern"
      pattern: "assets/scripts/logic"
---

<objective>
Set up Vitest test infrastructure and implement FlowerTypes, FlowerState, FlowerFSM, and Grid as pure TypeScript with full unit test coverage.

Purpose: All flower state logic and grid data model must exist as testable TypeScript before any renderer or game loop is built. This is the "pure logic tier" that Phase 3 renderer will read from but never mutate.

Output: vitest.config.ts, tsconfig.test.json, 4 source files, 3 test files — all passing with npm run test:run.
</objective>

<execution_context>
@C:/Users/Khac Linh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Khac Linh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-game-logic/02-CONTEXT.md
@.planning/phases/02-core-game-logic/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vitest infrastructure setup</name>
  <files>vitest.config.ts, tsconfig.test.json, package.json</files>
  <action>
    1. Check Node version: run `node --version`. If >= 20, install `vitest@^3` (latest). If 18-19, install `vitest@^2`. Run: `npm install -D vitest` at the project root (E:/workspace/ProjectAI).

    2. Read the existing `package.json`. Add these scripts WITHOUT removing any existing scripts:
       ```json
       "test": "vitest",
       "test:run": "vitest run",
       "test:coverage": "vitest run --coverage"
       ```

    3. Create `vitest.config.ts` at the project root:
       ```typescript
       import { defineConfig } from 'vitest/config';
       export default defineConfig({
           test: {
               environment: 'node',
               globals: true,
               include: ['assets/scripts/logic/**/*.test.ts'],
           },
       });
       ```

    4. Create `tsconfig.test.json` at the project root. Read the existing `tsconfig.json` first to get the correct base. The file should extend the base tsconfig but add Vitest globals only for test files:
       ```json
       {
           "extends": "./tsconfig.json",
           "compilerOptions": {
               "types": ["vitest/globals"]
           },
           "include": ["assets/scripts/logic/**/*.ts"]
       }
       ```
       Do NOT add `"types": ["vitest/globals"]` to the main `tsconfig.json` — that would pollute Cocos Creator's TypeScript compilation.

    5. Create the logic directory: `assets/scripts/logic/` (if it doesn't exist).

    6. Verify the setup works by running `npm run test:run`. It should exit with "no test files found" or "0 tests passed" — not with an error about missing modules or config issues.

    CRITICAL CONSTRAINT: Do not import from 'cc' in any file under assets/scripts/logic/. This is a hard boundary — Vitest cannot resolve the 'cc' virtual module.
  </action>
  <verify>
    <automated>cd E:/workspace/ProjectAI && npm run test:run 2>&1 | tail -5</automated>
  </verify>
  <done>npm run test:run exits without module resolution errors. vitest.config.ts and tsconfig.test.json exist at project root. Vitest is in package.json devDependencies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FlowerTypes, FlowerState, FlowerFSM</name>
  <files>
    assets/scripts/logic/FlowerTypes.ts,
    assets/scripts/logic/FlowerState.ts,
    assets/scripts/logic/FlowerFSM.ts,
    assets/scripts/logic/FlowerTypes.test.ts,
    assets/scripts/logic/FlowerFSM.test.ts
  </files>
  <behavior>
    FlowerTypes.test.ts:
    - All 5 FlowerTypeIds exist in enum: CHERRY, LOTUS, CHRYSANTHEMUM, ROSE, SUNFLOWER
    - FLOWER_CONFIGS[CHERRY].cycleDurationMs === 3000
    - FLOWER_CONFIGS[CHERRY].budMs === 1350 (3000 - 900 - 450 - 300)
    - FLOWER_CONFIGS[CHERRY].scoreBloom === 80
    - FLOWER_CONFIGS[CHERRY].scoreFull === 120
    - FLOWER_CONFIGS[SUNFLOWER].bloomingMs === 4670, fullBloomMs === 2330 (sum = 7000 exact)
    - All 5 configs: budMs + tapWindowMs + wiltingMs + deadMs === cycleDurationMs

    FlowerFSM.test.ts (use spawnTimestamp=0, inject fake nowMs):
    - getState(999) === BUD (CHERRY: budMs=1350ms, before tap window)
    - getState(1350) === BLOOMING (at budMs boundary)
    - getState(1350 + 600) === FULL_BLOOM (at bloomingMs boundary)
    - getState(1350 + 900) === WILTING (after tap window)
    - getState(1350 + 900 + 450) === DEAD (after wilting)
    - After collect(): getState(any) === COLLECTED
    - getScore(nowMs in BLOOMING) returns value between scoreBloom and scoreFull
    - getScore(1350) === 80 (scoreBloom at t=0 of tap window)
    - getScore(1350 + 900) is approximately 120 (scoreFull at end) — use toBeCloseTo
    - getScore(nowMs in BUD) === null
    - getScore(nowMs in WILTING) === null
  </behavior>
  <action>
    RED phase: Write FlowerTypes.test.ts and FlowerFSM.test.ts first (all tests should fail with "cannot find module").

    GREEN phase: Implement the source files.

    FlowerState.ts:
    ```typescript
    export enum FlowerState {
        BUD = 'BUD',
        BLOOMING = 'BLOOMING',
        FULL_BLOOM = 'FULL_BLOOM',
        WILTING = 'WILTING',
        DEAD = 'DEAD',
        COLLECTED = 'COLLECTED',
    }
    ```

    FlowerTypes.ts — export FlowerTypeId enum, FlowerTypeConfig interface, and FLOWER_CONFIGS constant with all 5 types. Use integer milliseconds throughout (no floats). SUNFLOWER: bloomingMs=4670, fullBloomMs=2330 (total 7000ms, not 4666+2334 — document this rounding in a comment). budMs for each type = cycleDurationMs - tapWindowMs - wiltingMs - deadMs. All values are locked from CONTEXT.md — do not change them.

    FlowerFSM.ts: Timestamp-based derivation — store only `_spawnTimestamp` and `_config` and `_collected: boolean`. `getState(nowMs)` computes elapsed = nowMs - spawnTimestamp then compares against cumulative thresholds. `getScore(nowMs)` returns null if not in tap window, else interpolates: `scoreBloom + (t / tapWindowMs) * (scoreFull - scoreBloom)` where t = elapsed - budMs. `collect()` sets _collected = true.

    No import from 'cc' in any file.
  </action>
  <verify>
    <automated>cd E:/workspace/ProjectAI && npm run test:run -- FlowerTypes.test.ts FlowerFSM.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>All FlowerTypes and FlowerFSM tests pass. FlowerState enum has 6 values. All 5 FLOWER_CONFIGS have correct integer ms values. budMs + tapWindowMs + wiltingMs + deadMs === cycleDurationMs for all 5 types.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Grid</name>
  <files>
    assets/scripts/logic/Grid.ts,
    assets/scripts/logic/Grid.test.ts
  </files>
  <behavior>
    Grid.test.ts:
    - new Grid() creates exactly 64 cells
    - cells[0]: index=0, row=0, col=0, flower=null
    - cells[63]: index=63, row=7, col=7, flower=null
    - getCell(3, 5) returns the cell at index 3*8+5 = 29
    - getCells() returns readonly array of 64 items
    - getRandomEmptyCell() on fresh grid returns a Cell (not null)
    - getRandomEmptyCell() on full grid (all cells have flower set) returns null
    - spawnFlower(cell, cherryConfig, 0) sets cell.flower to a FlowerFSM instance and returns it
    - After spawnFlower, that cell is no longer returned by getRandomEmptyCell
    - clearCell(cell) sets cell.flower = null
    - After clearCell, cell is again eligible for getRandomEmptyCell
    - getAliveCount(nowMs) returns count of cells where flower is not null and not DEAD
  </behavior>
  <action>
    RED phase: Write Grid.test.ts first.

    GREEN phase: Implement Grid.ts.

    Grid.ts: flat array of 64 Cell objects. Cell interface: `{ index: number; row: number; col: number; flower: FlowerFSM | null }`. Index math: `index = row * 8 + col`. Constructor initializes all 64 cells with flower=null.

    `getRandomEmptyCell()`: filter cells where flower===null, if none return null, else return random element.
    `spawnFlower(cell, config, nowMs)`: create `new FlowerFSM(nowMs, config)`, assign to cell.flower, return it.
    `clearCell(cell)`: set cell.flower = null.
    `getAliveCount(nowMs)`: filter cells where flower !== null AND flower.getState(nowMs) !== FlowerState.DEAD; return count. Import FlowerState from './FlowerState'.

    No import from 'cc'.
  </action>
  <verify>
    <automated>cd E:/workspace/ProjectAI && npm run test:run -- Grid.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>All Grid tests pass. Grid has 64 cells with correct index/row/col math. getRandomEmptyCell returns null when no empty cells exist. spawnFlower and clearCell mutate state correctly.</done>
</task>

</tasks>

<verification>
Run full test suite after all tasks:

```bash
cd E:/workspace/ProjectAI && npm run test:run 2>&1
```

All FlowerTypes, FlowerFSM, and Grid tests must be green. Zero import from 'cc' in any logic file:

```bash
grep -r "from 'cc'" E:/workspace/ProjectAI/assets/scripts/logic/ 2>/dev/null
```

Must return empty (no matches).
</verification>

<success_criteria>
- npm run test:run exits green with all tests passing
- FlowerFSM.getState(injected_nowMs) returns correct state for all 6 states
- FLOWER_CONFIGS has 5 entries; budMs + tapWindowMs + wiltingMs + deadMs === cycleDurationMs for each
- Grid has 64 cells; spawn/clear work; getRandomEmptyCell returns null when full
- vitest.config.ts and tsconfig.test.json exist; main tsconfig.json unchanged
- Zero 'cc' imports in assets/scripts/logic/
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-game-logic/02-01-SUMMARY.md` using the summary template.
</output>
